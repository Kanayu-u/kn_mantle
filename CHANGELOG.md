# Changelog

本プロジェクトのすべての注目すべき変更をこのファイルに記録する。
書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョニングは [Semantic Versioning](https://semver.org/lang/ja/) に従う。

## [1.0.0] - 2026-09-07

初回リリース。本番サーバーで**他プレイヤーの画面にも登攀動作として同期される**ことを確認した
（瞬間移動には見えない）。アンチチートの反応も無し。サーバー中継によるアニメーション同期は
不要と確定したため、Phase 0 のスパイクをそのまま製品版へ移行する。

掴みの判定ロジック自体は v0.4.1 から変更していない（実機で 18/18 成立した挙動をそのまま維持するため）。
変更点は既定値・公開範囲・ドキュメントに限る。

### Added
- `LICENSE` (GPL-3.0) を同梱。主要な Lua ファイルの先頭に著作権表示を付与
- README に由来（着想元は NoPixel V の mantle / 実装は全て自前）とバッジ 3 種、ライセンス節を追加

### Changed
- `debug` の既定値を `false` に変更
- `config/config.lua` を「運用者が触る値」と「内部値」に再編。
  上段: `locale` / `enabled` / `control` / `grabMaxHeight` / `grabMaxDistance` /
  `maxFallSpeed` / `assistWindowMs` / `debug`
- HUD のバージョン表記を fxmanifest から読むようにした（ハードコードの版数ずれを防ぐ）
- README.md を開発ログから利用者向けの文書へ全面的に書き直した

### Security
- 計測・調整用コマンド (`/mantlefall` `/mantlehold` `/mantlegate` `/mantlemax` `/mantlemin`
  `/mantledist` `/mantledebug` `/mantlereset`) を `Config.debug` が true のときのみ登録するようにした。
  これらは判定の帯や門番を実行時に変更できるため、本番でプレイヤーに開放してはならない。
  常時使えるのは `/mantle`（自分の掴みの ON/OFF）のみ。

### Removed
- `Config.cooldownMs` を削除。v0.3.0 で `maxAttemptsPerAir` + `retryMs` に置き換わっており、
  コードから参照されていなかった（設定できるのに効かない値だった）。
- `Config.debug` が false のときはデバッグ HUD スレッドを生成しないようにした
- 無条件の起動 `print` を削除（`Config.debug` のときのみ出力）

## [0.4.1] - 2026-09-06

実機計測で門番の上限 3.00 でも **issued:14 climb:14 none:0** (grab-h max:2.99 / rise max:2.70)。
掴み判定はペドのその瞬間の位置からの相対であり、**リーチ側に天井は無い**。
実際の上限を決めているのはジャンプの高さである、という結論に至った。

### Changed
- `grabMaxHeight` 2.40 → 3.00 (実測値を反映。跳んでも届かない壁を撃たないための足切りとして機能する)

### Added
- デバッグ表示中は地上でもレイキャストを回すようにした。跳ぶ前に「この壁は OK か」を確認できる。
  非滞空時のループは 250ms 間隔なので負荷は小さい。

## [0.4.0] - 2026-09-06

v0.3.0 の実機計測で **issued:4 climb:4 none:0 gated:59** を確認。
門番により無駄撃ち (= 謎の二段ジャンプ) が消え、狙った掴みが 4/4 で成立した。

高さの基準も検証できた。`rise:2.01` (GetEntityCoords の差分のみで算出) と
`grab-h:2.06` (レイキャスト + 足元基準で算出) が 0.05m 以内で一致しており、
独立した 2 経路が同じ値を出しているため基準は正しい。
**掴める縁の高さは 2.06m まで実績あり。上限は未特定** (門番の上限 2.40m を
通過したものが全て成功しているため、まだ天井に当たっていない)。

### Fixed
- 滞空中ずっと毎フレーム 5 本のレイキャストを撃っていた性能上の実害。
  `StartExpensiveSynchronousShapeTestLosProbe` は名前の通り高価であり、
  判定が必要なのはアシスト窓の間だけ。armed の間 (と HUD 表示中) のみ撃つようにした。

### Added
- 門番が止めた理由の内訳カウンタ `nowall:` / `noreach:`。
  門番が本来掴めるはずの縁を取りこぼしていないかを判別するため。
- 実行時に門番の帯を動かすコマンド `/mantlemax` `/mantlemin` `/mantledist`。
  登攀リーチの天井を再起動なしで探すためのもの。
- HUD に現在の門番の帯 `band:` を表示

## [0.3.0] - 2026-09-06

v0.2.0 の実機計測 (issued:17 climb:2 none:15 / at-try は全て空 / why:PENDING) を受けた修正。
**空中の TaskClimb が機能することは climb:2 で確認済み。** 以降は成立条件を詰める作業になる。

### Fixed
- 結果待ち (`pending`) が次の試行を塞いでいた設計バグ。滞空時間の大半で撃てていなかった。
  結果待ちは計測のみを担当し、発行を妨げないようにした。未解決のまま置き換える場合は空振りとして確定させる。

### Added
- レイキャストによる発行の門番 (`gateByProbe`)。前方に登れそうな縁が見えているときだけ `TaskClimb` を撃つ。
  **「掴む物が無いのに撃つ」ことが謎の二段ジャンプの正体だったため、これで解消される。**
- 押してから `assistWindowMs` (600ms) の間、条件が整い次第こちらで機会を拾うアシスト窓。
  成立フレームが狭く、押した瞬間はまだ壁に届いていないため、押すタイミングを人間に要求しない設計にした。
- 掴みが成立したときの縁の高さの計測 (`grab-h last/max`)。バニラの登攀リーチの実測値であり、Phase 1 の設計値になる。
- 高さの基準を `GetModelDimensions` の最下点から算出。`GetEntityCoords` がペドのどこを返すかに依存しない。
  HUD に頭の高さ (`head:`) を出し、基準が正しいか自己検証できるようにした (人型なら +1.6〜1.7m)。
- 門番の ON/OFF を切り替える `/mantlegate` (v0.2.0 の挙動との比較用)
- 門番が発行を止めた回数 `gated:` を HUD に追加

### Changed
- `retryMs` 90 → 50、`maxAttemptsPerAir` 8 → 20 (門番が無駄撃ちを止めるため緩めた)

## [0.2.0] - 2026-09-06

実機で「二段ジャンプは起きるが掴んで登らない」という結果を受けた修正。
バニラに空中ジャンプは無いため、この症状は TaskClimb が実行された上で
掴む対象を見つけられなかったことを意味する。

### Changed
- `allowDuringFall` の既定を `false` → `true` に変更。
  掴みが成立するのは跳んだ瞬間ではなく縁に触れる瞬間 (頂点〜下降中) であり、
  上昇中しか true にならない `IsPedJumping` だけを見ていた v0.1.0 は
  その窓を自分で塞いでいた。空中判定に `IsEntityInAir` / `IsPedFalling` を追加。
- 落下ダメージ回避への転用防止を `allowDuringFall` から `maxFallSpeed` (既定 12.0 m/s) へ移動。
  終端速度で縁を掴めない方が物理的にも自然で、通常のジャンプは阻害しない。
- 単発のクールダウンを、滞空 1 回あたりの試行回数上限 (`maxAttemptsPerAir`) へ変更。
  着地でリセットされる。

### Added
- ジャンプキー押しっぱなし中の再試行 (`holdRetry` / `retryMs`)。成立フレームが狭く単発では取りこぼすため
- 診断用レイキャスト: 前方 4 高さの壁までの距離と、その奥の「立てる天面」の高さ・距離を実測
  (まだ発行条件には使わない。Phase 1 の設計値を決めるための計測)
- HUD に落下速度 `vz`、試行回数、発行を見送った理由 `why`、試行時点のレイキャスト結果 `at-try` を追加
- ゲーム内コマンド `/mantlehold`

## [0.1.0] - 2026-09-06

Phase 0 スパイク。実機での挙動確認が目的であり、製品版ではない。

### Added
- 空中 (ジャンプ中 / 任意で落下中) にジャンプキーを再押下すると `TaskClimb` を発行する機能
- 連打によるマップ貫通・登攀キャンセルを防ぐクールダウンと状態ガード
  (車内 / ラグドール / 水泳 / 拘束 / 梯子 / パラシュート / 登攀中は発行しない)
- 発行後に `IsPedClimbing` / `IsPedVaulting` を監視し、実際に登攀へ入ったかを数える結果計測
- 登攀前後の高低差 (実測リーチ) の計測
- デバッグ HUD (状態フラグ・発行回数・成否内訳・リーチ)
- ゲーム内コマンド `/mantle` `/mantlefall` `/mantledebug` `/mantlereset`

### Known limitations
- 事前レイキャスト判定が無いため、掴む対象が無い場所では二段ジャンプのように見える (Phase 1 で対応)
- 他プレイヤーからの見え方 (タスク同期) は未確認
- 落下中の掴みは落下ダメージ回避に転用できるため既定で無効
